## What this changes


## Why


## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] The core (`src/core`) stays free of I/O and `stripe` imports
- [ ] The Stripe adapter only calls read endpoints (no writes)
- [ ] Tests added or updated for the change
